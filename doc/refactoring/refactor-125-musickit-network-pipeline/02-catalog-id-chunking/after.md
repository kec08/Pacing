# After — 카탈로그 ID 청크 요청

카탈로그 ID를 25개씩 나눠 각 요청의 limit과 일치시키고 모든 청크의 응답을 캐시·병합한다. 100개 ID가 모두 카탈로그에서 해석되는 조건에서는 요청 수가 `76회 → 4회`로 줄어 **94.7% 감소**한다.

이는 특정 응답 조건의 요청 수 비교이며, 실제 서버 latency·누락 응답률은 MusicKit 환경에 따라 달라진다.
