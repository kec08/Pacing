# After — AppDelegate 디스패치

`AppDelegate`를 `final class`로 제한했다. 상속 가능성을 제거해 의도를 명확히 하고 컴파일러가 정적 디스패치를 선택할 수 있게 했다.

Delegate 콜백은 앱 시작 경로라 독립적인 호출 비용을 신뢰성 있게 분리하기 어렵다. 따라서 퍼센트 성능 수치는 기록하지 않고, 컴파일 QA와 전체 클래스의 `final` 상태를 검증 기준으로 사용한다.
