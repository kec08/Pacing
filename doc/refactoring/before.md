# Refactor 117 Before

측정일: 2026-08-22

## 측정 대상

- 주간 통계: `filter`가 만든 중간 배열에 대해 `reduce`를 수행
- 경로 세그먼트: `compactMap` 결과 배열과 세그먼트별 `Array` 복사 사용
- Static Dispatch: `AppDelegate`가 `final`이 아닌 일반 클래스

## 재현 조건

- 벤치마크: `doc/refactoring/benchmark.swift`
- 최적화: `swiftc -O`
- 입력: 러닝 기록 100,000개, 경로 좌표 100,000개
- 각 측정: 30회 반복, 프로세스 5회 실행
- 네트워크·Firebase·UI 렌더링 시간 제외

## 기준선

| 경로 | 평균 실행 시간 |
| --- | ---: |
| 경로 세그먼트 생성 | 0.04631 ms |
| 유효 러닝 통계 집계 | 0.36086 ms |

기준선 계산은 중간 배열을 실제로 생성하는 기존 로직으로 측정했다. 두 경로 모두 결과 checksum을 함께 확인해 최적화 전후의 계산 결과를 비교할 수 있게 했다.
