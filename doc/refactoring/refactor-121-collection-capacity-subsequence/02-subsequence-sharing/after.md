# After — Sub-sequence Sharing

필요한 배치 범위만 `ArraySlice`로 생성해 원본 `tracks` 버퍼를 공유하며 순회한다. 배치 배열 목록과 각 배치의 복사를 제거했다.

100,000개 입력을 4개씩 나누는 순수 벤치마크에서 `1.36792 ms → 0.00145 ms`로 **99.9% 개선**됐다. 이는 배치 배열 25,000개의 복사를 제거한 결과이며, 실제 MusicKit 요청 시간은 포함하지 않는다.
